networks:
  devicePurcahsedNet:
    external: true

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:4000}
    container_name: purchase_dashboard_frontend
    restart: always
    ports:
      - "${FRONTEND_PORT:-80}:80"
    networks:
      - devicePurcahsedNet
