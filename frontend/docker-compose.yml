networks:
  devicePurcahsedNet:
    external: true

services:
  frontend:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://scleraai.tail9bdf21.ts.net/api}
    container_name: purchase_dashboard_frontend
    restart: always
    ports:
      - "3001:80"
    networks:
      - devicePurcahsedNet
