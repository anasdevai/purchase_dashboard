# Copy to frontend/.env before: docker compose build && docker compose up -d
#
# Optional — only needed when API is on a different host than the UI.
# Same-host /api proxy (recommended): leave unset; nginx proxies to the backend container.
# VITE_API_URL=https://scleraai.tail9bdf21.ts.net/api

FRONTEND_PORT=80
